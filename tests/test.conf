NUM_NODES=3
POOL_SIZE=128M

# ZEP settings
POLICY="fail"

# ZFS settings
ZFS_FORCE="false"
ZFS_RATE="1M"

# SMTP settings
SMTP_FROM="zep@local"
SMTP_HOST="127.0.0.1"
SMTP_PASSWORD="none"
SMTP_PORT="1025"
SMTP_PROTOCOL="smtp"
SMTP_STARTTLS="false"
SMTP_TO="admin@local"
SMTP_USER="none"
