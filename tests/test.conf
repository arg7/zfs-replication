NUM_NODES=3
POOL_SIZE=128M

# ZEP settings
CHAIN="node1,node3,node2"
POLICY="fail"

# ZFS settings
ZFS_FORCE="false"
ZFS_RAW="false"
ZFS_RESUME="false"
ZFS_THROTTLE="64k"

# SMTP settings
SMTP_FROM="zep@local"
SMTP_HOST="127.0.0.1"
SMTP_PASSWORD="none"
SMTP_PORT="1025"
SMTP_PROTOCOL="smtp"
SMTP_STARTTLS="false"
SMTP_TO="admin@local"
SMTP_USER="none"

# Alert settings
ALERT_CRITICAL_THRESHOLD="0"
ALERT_WARN_THRESHOLD="0"
ALERT_INFO_THRESHOLD="0"
