NUM_NODES=3
POOL_SIZE=128M

# ZEP settings
CHAIN="node1,node3,node2"
POLICY="fail"

# ZFS settings
ZFS_FORCE="false"
ZFS_RATE="1M"

# SMTP settings
SMTP_FROM="zep test"
SMTP_HOST="localhost"
SMTP_PASSWORD="your password"
SMTP_PORT="465"
SMTP_PROTOCOL="smtps"
SMTP_STARTTLS="false"
SMTP_TO="zep user"
SMTP_USER="your user"
